export type AuthFormType = {
    username: string;
    password: string;
  
};

export type Character = {
    id: string;
    name: string;
    gender: string;
    status: string;
    species: string;
  }